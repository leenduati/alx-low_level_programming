#include <stdio.h>

/**
 *subNumbers: accepts to inputs and returns subtraction
 *@x: input as an integer
 *@y: input as an integer
 *Return: addition of two numbers
*/

int subNumbers(int x, int y)
{
	return (x - y);
}
