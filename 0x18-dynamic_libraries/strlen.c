#include <string.h>
#include <stdio.h>

/**
 * _strlen- Outputs length of a string
 *@s : parameter passed*
 * Return: Always a (Success)
 */


int _strlen(char *s)
{
	int p;

	p = strlen(s);
	return (p);
}
