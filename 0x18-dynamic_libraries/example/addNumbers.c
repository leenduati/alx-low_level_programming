#include <stdio.h>

/**
 *addNumbers: accepts to inputs and returns addition
 *@x: input as an integer
 *@y: input as an integer
 *Return: addition of two numbers
*/

int addNumbers(int x, int y)
{
	return (x + y);
}
