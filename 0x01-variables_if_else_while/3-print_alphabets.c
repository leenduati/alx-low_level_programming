#include <stdio.h>
/**
 * main - Program entry point
 *
 * Return: 0 indicates success
 */

int main(void)
{
	char a;
	/* Program */
	for (a = 'a'; a <= 'Z'; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
