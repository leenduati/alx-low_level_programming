#include <stdio.h>
#include <string.h>
#include "main.h"

void print_diagsums(int *a, int size)
{
	int b;
	int c;
	int sum_r;
	int sum_l;
	char *res;

	for (b = 0; b < size; b++)
	{
		for (c = 0; c < size; c++)
		{
			sum_1 = 
