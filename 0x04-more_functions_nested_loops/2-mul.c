#include "main.h"
#include <stdio.h>

/**
 * mul -multiplies too numbers
 *@a : parameter passed*
 *@b : parameter passed*
 * Return: Always a (Success)
 */


int mul(int a, int b)
{

	return (a * b);
}
